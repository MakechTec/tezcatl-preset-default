@foreach(import)
import ${import} from '';
@endif

@if(class)
export default class ${component}{

}
@endif
@if(const)
export const ${component} = {
    
};
export default ${component};
@endif
@if(func)
export const ${component} = () => {
    return;
};

export default ${component};
@endif

@foreach(const)
export const ${const} = () => {
	return (
		<div>
			<h1>${const}</h1>
		</div>
	);
};
@endforeach

@foreach(func)
export const ${func} = () => {
    return;
};
@endforeach